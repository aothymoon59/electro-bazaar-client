export type TQuery = {
  minPrice: number;
  maxPrice: number;
  releaseDate: string;
  brand: string;
  modelNumber: string;
  category: string;
  operatingSystem: string;
  connectivity: string;
  powerSource: string;
  cameraResolution: number;
  storage: number;
};
