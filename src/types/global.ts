export type TQueryParam = {
  name: string;
  value: boolean | React.Key;
};

export interface userData {
  _id: string;
  name: string;
  email: string;
  role: string;
  status: string;
  isDeleted: string;
}
